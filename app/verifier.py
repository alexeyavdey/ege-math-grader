"""Независимая проверка корректности задания и ответа средствами sympy.

⚠️ ЭТОТ МОДУЛЬ НУЖНО ДОПИСАТЬ (часть тестового задания).

Идея: нельзя доверять полю `answer` из генератора (и тем более вердикту LLM).
Истинные корни должны вычисляться символически из самого условия задачи — и уже
с ними сверяется как сгенерированный ключ, так и ответ ученика.
"""

from __future__ import annotations


def solve_from_statement(statement: str) -> list[str]:
    """Вернуть реальные корни уравнения, разобранного из текста условия.

    TODO: реализовать.
    Подсказки: извлечь уравнение из строки, привести к sympy-синтаксису
    ('^' -> '**', '5x' -> '5*x'), затем sympy.Eq / sympy.solve. Вернуть корни
    отсортированными и в том же текстовом формате, что ожидается в ответе.
    """
    raise NotImplementedError


def verify_task(statement: str, declared_answer: str) -> bool:
    """True, если declared_answer совпадает с реально вычисленными корнями.

    TODO: реализовать. Используется в двух местах:
      • при генерации — чтобы не отдать ученику задание с неверным ключом;
      • при проверке ответа — как источник истины вместо вердикта LLM.
    """
    raise NotImplementedError
